# P0 / P1 验收记录

- 验收日期：2026-07-29
- 验收范围：`YS-P0-01`、`YS-P1-01—05`
- 浏览器：Codex 应用内置 Chromium（控制接口未暴露具体 Chromium 版本号）
- 构建样本：隔离目录中的静态生产导出；原工作目录的 `.next` 仍受 Windows 文件锁影响

## 人工试玩记录

| 起点 | 实际路径 | 核对结果 |
|---|---|---|
| 新档 | 第一章全部 84 节点；完成分类、纸背查验、版本比对、残片拼接；结尾选“同行” | 四类职业动作均可完成；叙事从日常佣书逐步转入史料风险；结尾保留“第十三页来源”“门外来客身份”“沈掌柜旧友与遗书目的”三组可继续追查的悬念；没有在第一章提前宣告南海目的地 |
| 同行结尾 | 完整阅读结尾尾声 → 第二章《旧友遗稿》→ 南海路线 → 当面交还周家 → 著录为交还 → 终页 → 舆图 | 南海由第二章地图首次揭示；`zhou-interleaf` 原件状态为 `transferred`，返回前页仍不可恢复；西樵入口形成；终页与舆图可达；控制台无游戏错误 |
| 新档 | 第一章全部 84 节点；完成四项职业活动；结尾选“留守” | 完整留守尾声可读，进入广州时没有跳过第一章收束；书坊独守压力与陌生客悬念成立 |
| 留守结尾 | 第二章《书坊旧稿》→ 广州路线 → 旧志列入存疑 → “暂缓判断”终页 → 舆图 | 银钱未被额外结算，风险按路线增加；原件没有被错误标为销毁或出售；广州修志局入口以 `guangzhou-gazetteer-office` 独立状态保存，而不是重复解锁广州；终页与舆图可达；控制台无游戏错误 |
| 已写入书签 | 第一章第 1 节推进到第 2 节 → 返回卷首 → “继续游戏” | 续读恢复到第 2 节；书签状态提示正常；设置页可切换减少动态；控制台错误为 0 |

说明：第二章其他结局的永久处置、银钱、风险和入口组合由领域／故事数据测试逐项覆盖；人工试玩选择上述两条代表性路线。浏览器刷新若仍带 `mode=new` 会按产品定义开始新档，因此恢复验证从卷首使用“继续游戏”，没有把强制新档 URL 当成续读入口。

## 自动化与构建证据

- `npm.cmd test`：26 / 26 通过。
- `npm.cmd run lint`：通过，0 warning。
- `npx.cmd tsc --noEmit --incremental false`：通过。
- `npm.cmd run build`：同一源码在无旧缓存的隔离目录完成；8 / 8 静态页面生成，`/game`、`/archive`、`/map`、`/settings` 均输出。
- 故事校验覆盖：章节注册、全图可达、跨章／同章目标、终点、地点、史料、档案 `unlockArchive`、路线入口 ID。
- 存档校验覆盖：schema v1、v0 迁移、嵌套长编结构、永久处置、路线入口迁移／往返、损坏 JSON、未来版本、配额失败。

## P0 审校结论

- `HIST-001—005` 均已标明 H1/H2/H3/H4、来源层级、母库相对路径、PDF 页码、审校者与边界。
- 曾德昭相关材料的现代研究存在 11 月 24 / 25 日概括差异；玩家残页改用董少新译注第262页的短段删节，不把任一精确公历日伪装成葡文原稿正文。
- 澳门传播链只计同一研究链一次；“寄澳门—1653 年编纂—十八世纪抄本”作为后台研究链记录，不在当前节点冒充多条独立证据。

## 未纳入 P0 / P1 的验证

- iPhone Safari、微信内置浏览器、Firefox、200% 缩放、完整键盘与真机触摸回归属于 P2 发布门禁；根据创始人 2026-07-29 的后续范围决定，Android Chrome 由原硬门禁降为非阻断未验证项，不得宣称已兼容。
- 原工作目录 `.next` 的 Windows 文件锁清理属于环境维护项；不以其替代隔离生产构建，也不把它误判为源码失败。
